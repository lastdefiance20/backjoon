import sys

def permutation(N, M):
    save = []
    
    for i in range(M):
        for x in range(N):
            if

N, M = map(int, sys.stdin.readline().split())



for w in range(M):
    for x in range(1, N+1):
        print(x, end=" ")